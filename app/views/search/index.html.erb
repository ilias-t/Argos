<div id="map">
</div>

<form id="search">
  <input id="search_term"></input>
</form>



</div>
<div id="address"></div>

<script>
    var map, geocoder;

    $("#search").appendTo("body");

    $("#search").on("submit", function(e){
      e.preventDefault();
    });

    // function to get all related places

    // pin each place


    require([
      "esri/map", "esri/dijit/Geocoder", "dojo/domReady!", "esri/symbols/SimpleMarkerSymbol"
      ], function(Map, Geocoder) {
        map = new Map("map",{
          basemap: "gray",
          center: [-98.35, 39.50],
          zoom: 4
        });

        geocoder = new Geocoder({ 
          map: map 
        }, "address");

        geocoder.startup();

        var point = new Point;

        marker = new SimpleMarkerSymbol({
          "xoffset": 0,
          "yoffset": 0
        });
      });
</script>